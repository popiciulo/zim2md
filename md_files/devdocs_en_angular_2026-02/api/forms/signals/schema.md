# schema

schema



# schema

Creates a [`Schema`](schema) that adds logic rules to a form.

## API

```
function schema<TValue>(fn: SchemaFn<TValue>): Schema<TValue>;
```

### schema

`Schema<TValue>`

Creates a [`Schema`](schema) that adds logic rules to a form.

@paramfn`SchemaFn<TValue>`

A **non-reactive** function that sets up reactive logic rules for the form.

@returns`Schema<TValue>`

Super-powered by Google ©2010–2025.  
Code licensed under an MIT-style License. Documentation licensed under CC BY 4.0.  
<https://angular.dev/api/forms/signals/schema>
